<!DOCTYPE html>
<html>
<head>
  <title>SwTrade</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<style type="text/css">
.typeahead{
  width: 76.2em
}
</style>
<div class="container">

  <div class="masthead">
    <ul class="nav nav-justified">
      <li class="menu active" id="Home"><%= link_to 'Home', root_path %></li>
      <li class="menu"><a href="#">Projects</a></li>
      <li class="menu"><a href="#">Services</a></li>
      <li class="menu"><a href="#">Downloads</a></li>
      <li class="menu"><a href="#">About</a></li>
      <li class="menu" id="Contact"><%= link_to 'Contact', contact_path %></li>
    </ul>
  </div>
  <div style="margin-top: 2em; margin-bottom: 1em">
    <%= form_tag search_path, :method => 'post', role: "form", class: "form-inline" do %>
      <%= text_field_tag :search, params[:search], class: "form-control ", id: "search", autocomplete: "off",placeholder: "Search Monster Here", style: "width:75.9em", autocomplete: 'off', :data => {:provide => "typeahead"} %>
      <!--<input type="text" data-provide="typeahead" id="search" autocomplete="off">-->
      <%= submit_tag "search", :name => :nil, class: "btn btn-default"%>
    <% end %>
  </div>

  <% flash.each do |type, message| %>
    <div class="alert alert-warning" role="alert">
      <%= message %>
    </div>
  <% end %>

  <!-- Example row of columns -->
  <!--<div class="row">
    <div class="col-lg-4">
    </div>
  </div>-->
  <%= yield %>

  <!-- Site footer -->
  <div class="footer">
    <p>© Company 2014</p>
  </div>
</div>

</body>
</html>
