<%= form_for([:admin, @acc_photo]) do |f| %>
  <% if @acc_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@acc_photo.errors.count, "error") %> prohibited this acc_photo from being saved:</h2>

      <ul>
      <% @acc_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :account_id %><br>
    <%= f.number_field :account_id %>
  </div>
  <div class="field">
    <%= f.label :photo %><br>
    <%= f.text_field :photo %>
  </div>
  <div class="field">
    <%= f.label :default %><br>
    <%= f.check_box :default %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
